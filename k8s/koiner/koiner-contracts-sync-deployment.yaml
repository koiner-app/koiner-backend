apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: koiner-contracts-sync
  name: koiner-contracts-sync
  namespace: koiner
spec:
  replicas: 1
  selector:
    matchLabels:
      service: koiner-contracts-sync
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        service: koiner-contracts-sync
    spec:
      containers:
        - name: koiner-contracts-sync
          image: elimbatido/koiner-contracts-sync:v0.1.1
          env:
            - name: NODE_ENV
              value: "production"

            - name: DB_HOST
              value: [INSERT]
            - name: DB_PORT
              value: "25060"
            - name: DB_NAME
              value: koiner-contracts
            - name: DB_USER
              value: koiner-contracts
            - name: DB_PASSWORD
              value: [INSERT]
            - name: DB_TYPE
              value: postgres
            - name: SSL_CERT
              value: '[INSERT]'
            - name: PORT
              value: "3102"
            - name: POSTGRES_SYNC
              value: "false"

            - name: RABBITMQ_HOST
              value: amqp:5672
            - name: RABBITMQ_USER
              value: guest
            - name: RABBITMQ_PASSWORD
              value: guest

              # '[https://jsonrpc.api.koiner.app]'
            - name: JSONRPC_NODES
              value: '["https://api.koinos.io"]'
#          resources:
#            requests:
#              memory: "64Mi"
#              cpu: "250m"
#            limits:
#              memory: "128Mi"
#              cpu: "500m"
          ports:
            - containerPort: 3102
          readinessProbe:
            tcpSocket:
              port: 3102
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3102
          imagePullPolicy: Always
      serviceAccountName: default
