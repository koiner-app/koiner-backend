{
  "$schema": "..\\..\\..\\node_modules\\nx\\schemas\\project-schema.json",
  "sourceRoot": "apps/graphql/gateway/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "mesh --dir apps/graphql/gateway build"
          }
        ],
        "poll": 2000
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "mesh --dir apps/graphql/gateway start"
          }
        ]
      }
    },
    "serve-dev": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "mesh --dir apps/graphql/gateway dev"
          }
        ]
      }
    }
  },
  "tags": []
}
